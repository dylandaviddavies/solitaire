/**
 * The dashed rounded outline shared by every pile-shaped affordance: the
 * empty-pile placeholder and the drag-time drop-zone hint. Kept in one
 * place so the two never drift apart. Callers add their own border colour.
 */
export const DASHED_PILE_OUTLINE = 'rounded-2xl border-[3px] border-dashed'
